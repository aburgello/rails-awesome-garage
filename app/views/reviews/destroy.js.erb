const reviewId = '<%= @review.id %>'; // This should be the ID of the review being deleted
const reviewElement = document.getElementById(`review_${reviewId}`);

if (reviewElement) {
  reviewElement.remove(); // Remove the review from the DOM
}

// You can also return a success message, but the alert should be handled in the JS code
