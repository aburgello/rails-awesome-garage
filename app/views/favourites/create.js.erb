// create.js.erb
Swal.fire({
  title: 'Added to Favorites',
  text: 'You have added <%= @car.brand %> <%= @car.model %> to your favourites, great choice!',
  icon: 'success',
  confirmButtonText: 'OK',
});

console.log("Favourite added:");

// Update the favorites form to show the "Remove from Favourites" button
if (typeof formContainer == 'undefined'){
const formContainer = document.querySelector('.ajax-favourites-form');
if (formContainer) {
  formContainer.innerHTML = `
    <h3 class="text-xl font-semibold mt-6">This car is in your favourites</h3>
    <form action="<%= car_favourite_path(@car, @favourite) %>" method="post" data-remote="true" local="false" >
      <input type="hidden" name="_method" value="delete">
      <input type="submit" value="Remove from Favourites" class="mt-2 bg-red-500 text-white p-2 rounded hover:bg-red-600 cursor-pointer">
    </form>
  `;
}
}