<div class="container mx-auto p-4">
  <h1 class="text-4xl font-bold mb-4">Your Favourite Cars</h1>
  <p class="text-gray-600 mb-6">Here are the cars you have marked as favorites:</p>

  <% if @favourites.any? %>
    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      <% @favourites.each do |favourite| %>
        <li class="bg-white shadow rounded-lg p-4">
          <img src="<%= favourite.car.image_url %>" alt="<%= favourite.car.brand %> <%= favourite.car.model %>" class="w-full h-48 object-cover rounded mb-4">
          <h2 class="text-xl font-semibold"><%= favourite.car.brand %> <%= favourite.car.model %></h2>
          <p class="text-gray-500">Year: <%= favourite.car.year %></p>
          <p class="text-gray-500">Fuel: <%= favourite.car.fuel %></p>
          <%= link_to 'View Details', car_path(favourite.car), class: 'text-blue-500 hover:underline mt-2 inline-block' %>
          
          <%= form_with url: car_favourite_path(favourite.car, favourite), method: :delete, local: true, class: 'mt-4' do %>
            <%= submit_tag "Remove from Favourites", class: 'bg-red-500 text-white p-2 rounded hover:bg-red-600' %>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-gray-600">You have no favorite cars at the moment.</p>
  <% end %>
  
  <%= link_to 'Back to Cars List', cars_path, class: 'mt-4 inline-block text-blue-500 hover:text-blue-700' %>
</div>
